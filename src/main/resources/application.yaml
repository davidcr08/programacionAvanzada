spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/
      database: MyBaseAvanzada

# Configuración de Spring Boot para conectarse a MongoDB local
# Ya no se usa contraseña aquí (no se requiere password por defecto en localhost MongoDB)
